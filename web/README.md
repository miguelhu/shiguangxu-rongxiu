# 荣休礼展示网站工程

这是原荣休礼互动网站的独立源码副本，保留案例选择、共创者／统筹者／长者视角、生命周期、并排对照和讲解。运行 `npm ci && npm run dev`，访问 `http://localhost:5188/`；`npm run build` 会检查类型、生成 `dist/` 与独立演示 HTML。

网站入口为 `src/v6/App.tsx`。这里的角色切换、讲解和案例数据是展示工具，不是正式小程序或相框产品界面。真实产品需求见 [荣休礼 PRD](../docs/PRD/README.md)。本目录是源码交接，不与已部署线上网站自动同步。
